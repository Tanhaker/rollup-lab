{
  "name": "rollup-lab",
  "version": "1.0.0",
  "private": true,
  "description": "A four-page field guide to Ethereum Layer 2 and Arbitrum. The site itself has no build step — this file exists only for the dev tooling below.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "contrast": "node tools/contrast-check.js",
    "check": "npm test && npm run contrast",
    "serve": "python3 -m http.server 5500"
  },
  "keywords": ["arbitrum", "ethereum", "layer2", "rollup", "web3"],
  "license": "MIT"
}
